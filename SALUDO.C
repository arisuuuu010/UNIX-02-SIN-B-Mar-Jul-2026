//start the code with a library
#include <stdio.h>
// C is a typed language so, when we programmin here, we must specify the int
// Main function returns an integer to the OS
int main() {
    printf("Hello word!/n");
    // Return 0 means the program finished successfully
    return 0;

}
//pwd (print work directory)