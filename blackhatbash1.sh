#!/bin/bash -x 
#(It shows the commands being executed)
#--Commands---
 bash --version
env
echo ${SHELL
echo ${RANDOM}}
echo ${UID}
echo ${OSTYPE}
ps -ef
 df --human-readable
#We can execute the script in restricted mode using -r option
 bash -r myscript.sh